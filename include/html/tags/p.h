#pragma once

#include <iostream>

#include "../../main.h"

namespace browser {
    namespace elements {
        class p: browser::elements::element {
            private:
            public:
        };
    }
}